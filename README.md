# 🛫 WizzAir Price Checker Bot

Этот бот автоматически отслеживает цены на авиабилеты компании **WizzAir** и отправляет уведомления о изменениях в Telegram.

---

## ⚙️ Настройка маршрутов

Все маршруты для отслеживания задаются в файле **`routes.json`**.  
Пример структуры файла:

```json
[
  {
    "id": "rmo-otp-2025-12-25",  
    "origin": "OTP",             
    "destination": "TFS",        
    "depart_date": "2026-07-08", 
    "return_date": null,         
    "adults": 1,                 
    "cabin_class": "economy"     
  },
  {
    "origin": "TFS",
    "destination": "OTP",
    "depart_date": "2026-07-08",
    "return_date": null,
    "adults": 1,
    "cabin_class": "economy"
  },
  {
    "origin": "RMO",
    "destination": "OTP",
    "depart_date": "2026-07-08",
    "return_date": null,
    "adults": 1,
    "cabin_class": "economy"
  }
]
```
Пояснения к полям:

**Пояснения к полям:**

| Поле | Назначение |
|------|-------------|
| `id` | Можно не трогать — не участвует в поиске |
| `origin` | Откуда вылет (код аэропорта, см. `airports.json`) |
| `destination` | Куда летим (код аэропорта, см. `airports.json`) |
| `depart_date` | Дата вылета _(год-месяц-день)_ — желательно взять дату с сайта WizzAir |
| `return_date` | Обратная дата. Если не нужна — оставить `null` |
| `adults` | Количество взрослых пассажиров |
| `cabin_class` | Класс билета (`economy` и т.д.) |


Если вы хотите удалить лишний маршрут, уберите всю запись объекта вместе с запятой после него.

🤖 Настройка Telegram

В BotFather создайте нового бота.

Получите TELEGRAM_TOKEN и запишите его в файл .env.

Узнайте свой CHAT_ID (через getUpdates или любого бота-помощника).

Пример файла .env:
```
TELEGRAM_TOKEN=1234567890:ABCdefGhIJKlmNoPQRstuVWxyz
CHAT_ID=123456789
```
🚀 Запуск

Можно запустить:

локально — на компьютере, который не выключается;

на сервере (VPS) — для постоянного мониторинга.

Пример запуска:

```
python3 wizzair_checker.py

```
💡 Как это работает

Скрипт регулярно проверяет цены для всех маршрутов из routes.json.

Сравнивает новые цены с предыдущими (сохраняются в prev_prices.json).

При изменении цены отправляет сообщение в Telegram.

```
.
├── routes.json          # список маршрутов для мониторинга
├── prev_prices.json     # сохранённые прошлые цены
├── .env                 # токен и ID чата Telegram
├── wizzair_checker.py   # основной скрипт
└── airports.json        # справочник аэропортов (по IATA-кодам)

```
